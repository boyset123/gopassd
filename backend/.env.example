MONGODB_URI=
JWT_SECRET=

# Local dev — Gmail App Password (works on your machine)
EMAIL_USER=gopassdorsu@gmail.com
EMAIL_PASS=

# Render production — Gmail SMTP is blocked on free tier; use Resend instead (HTTPS)
# Sign up at https://resend.com → API Keys → add to Render → Environment
RESEND_API_KEY=
# After verifying your domain in Resend (optional):
# EMAIL_FROM=GoPass DOrSU <noreply@yourdomain.edu.ph>
