# Optional: in Render → New → Blueprint, connect this repo and apply.
# Live service URL: https://gopassd.onrender.com
services:
  - type: web
    name: gopassd
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
